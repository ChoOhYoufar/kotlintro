package classes

/**
 * @author cho.oh 2018/07/30.
 */
//good kotlin class only have one constructor with default parameters for optional values
// just use one primary constructor and init blocks

//secondary constructor必要な場合は、help classを作る
//fun makeDefaultFish() = Fish(true, 50)